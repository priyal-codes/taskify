<nav class="navbar navbar-expand-md sticky-top">
    <div class="container">
        <a class="navbar-brand" href="/tasks">
            <i class="fa-solid fa-layer-group"></i> 
            Taskify
        </a>
        <button class="navbar-toggler border-0 text-secondary" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup">
            <i class="fa-solid fa-bars"></i>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav me-auto">
                <a class="nav-link" href="/tasks"><i class="fa-solid fa-table-columns me-1 small"></i> Workspace</a>
                <a class="nav-link" href="/tasks/new"><i class="fa-solid fa-plus me-1 small"></i> New Task</a>
            </div>
            <div class="navbar-nav ms-auto align-items-md-center gap-2">
                <button id="themeToggleBtn" class="theme-toggle-btn" title="Toggle Dark / Light Mode" aria-label="Toggle Dark Mode">
                    <i class="fa-solid fa-moon" id="themeIcon"></i>
                </button>
                <% if (!locals.currentUser) { %>
                    <a class="btn btn-outline-dark" href="/login">Login</a>
                    <a class="btn btn-dark" href="/signup">Sign Up</a>
                <% } else { %>
                    <span class="nav-link font-mono small text-secondary">
                        <i class="fa-regular fa-user me-1"></i> <%= currentUser.username %>
                    </span>
                    <a class="btn btn-outline-dark" href="/logout">Logout</a>
                <% } %>
            </div>
        </div>
    </div>
</nav>